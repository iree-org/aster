# Load Unit test config when lit enters this directory.
# Adapted from LLVM/MLIR upstream (test/Unit layout).
# Unit tests are C++ Google Test executables, not .mlir files.
import os
lit_config.load_config(
    config,
    os.path.join(config.aster_obj_root, "test", "Unit", "lit.site.cfg.py"),
)
