# Build/test requirements.
cmake>=3.31.6
ninja
lit>=18.1.7
setuptools>=70.2.0
wheel
black

# Taken from MLIR's requirements.txt
# TODO: Keep in sync automatically.
nanobind>=2.9, <3.0
PyYAML>=5.4.0, <=6.0.1
typing_extensions>=4.12.2
numpy>=2.1.0, <=2.1.2
ml_dtypes>=0.5.0, <=0.6.0

# Used for managing pre-commit flows.
pre-commit
pytest
pytest-xdist

# Type checking
pyright

# Progress bar
tqdm
